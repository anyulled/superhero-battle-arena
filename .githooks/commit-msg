#!/usr/bin/env sh

set -eu

if [ ! -x node_modules/.bin/commitlint ]; then
  echo "Install the pinned harness dependencies with npm ci before committing." >&2
  exit 1
fi

exec node_modules/.bin/commitlint --config commitlint.config.mjs --edit "$1"
